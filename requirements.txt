# ============================================================
# DAgent 企业级多智能体框架 —— 后端依赖
# 版本组合为实测验证过的可用组合（不要随意升级）
#   ⚠️ 两个关键锁：fastmcp==2.14.7 + mcp==1.26.0
#      升级 fastmcp 到 3.x 或 mcp 到 1.29+ 会踩 SSE/握手兼容 bug
# ============================================================

# Agent 框架（LangGraph 状态图 + 多阶段 Harness 的封装）
deepagents==0.7.10
# LangChain 生态（1.x，与 deepagents 0.7 配套）
langchain==1.3.18
langchain-core==1.6.1
# OpenAI 兼容接口（本项目用它接阿里云通义千问 qwen-plus）
langchain-openai==1.6.0
# 连接 MCP Server 的工具加载器
langchain-mcp-adapters==0.3.2
# LangGraph 运行时（astream / checkpointer / interrupt）
langgraph==1.2.11

# ---- MCP 桥接层（Agent ↔ 业务工具）----
# fastmcp：MCP Server 框架（提供工具给 Agent）
fastmcp==2.14.7
# mcp：MCP 协议 SDK（fastmcp 底层依赖，版本锁 1.26.0）
mcp==1.26.0

# ---- Web 服务层 ----
fastapi>=0.115.0        # API 框架
uvicorn>=0.30.0         # ASGI 服务器
httpx>=0.28.0           # HTTP 客户端（MCP client 底层用）
python-dotenv>=1.0.0    # .env 配置读取
pydantic>=2.0.0         # 数据模型
